{
  "buildCommand": "cd ../.. && npm install && turbo run build --filter=web",
  "outputDirectory": "apps/web/dist",
  "framework": null,
  "installCommand": "echo 'Skipping install, handled in buildCommand'"
}
